const initialState = {

};

const createDeckReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':
        default:
            return state
    }
};

export default createDeckReducer;