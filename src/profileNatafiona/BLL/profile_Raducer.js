
let inisialState = [];

let profileReducer  = (state=inisialState,action ) =>{
    switch(action.type){
        case '':return; 
        default:return state
    }

}

export default profileReducer